# Sensitive identity

Copy this file to `sensitive-knowledge.md`. That file stays private and is not
committed or copied into the Docker image.

Store only identity data that a legitimate recipient may require. Never store
passwords, payment card details, PINs, or authentication codes.

- DNI:
